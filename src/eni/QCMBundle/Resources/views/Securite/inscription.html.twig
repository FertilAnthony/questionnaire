{% extends 'eniQCMBundle::layout.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block content %}
<form id="inscription-utilisateur" action="{{ path('inscription') }}" method="POST" class="form-horizontal" role="form">
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword) }}
    {{ form_row(form.repeatPassword) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.nom) }}

    {{ form_rest(form) }}

    <div class="form-group">
        <div class="col-sm-offset-3 col-sm-9">
            <button type="submit" class="btn btn-default valider">Valider</button>
        </div>
    </div>
</form>
{% endblock %}